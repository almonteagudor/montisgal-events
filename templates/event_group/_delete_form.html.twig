<form method="post" action="{{ path('app_event_group_delete', {'id': event_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_group.id) }}">
    <button class="btn">Delete</button>
</form>
