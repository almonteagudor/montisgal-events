{% extends 'super_admin/base.html.twig' %}

{% block title %}User index{% endblock %}

{% block content %}
    <div class="d-flex justify-content-between align-items-center my-3">
        <h1>User index</h1>
        <a href="{{ path('super_admin_user_new') }}" class="btn btn-success">Create new</a>
    </div>

    <hr>

    <div class="d-flex flex-wrap justify-content-between">
        {% for user in users %}
            {{ include('super_admin/user/_card.html.twig', {'user': user}) }}
        {% else %}
            no records found
        {% endfor %}
    </div>
{% endblock %}
